diff --git a/.gitattributes b/.gitattributes
new file mode 100644
index 0000000..08b43db
--- /dev/null
+++ b/.gitattributes
@@ -0,0 +1,2 @@
+ssh-low-interaction/ssh-low-interaction-server.tar filter=lfs diff=lfs merge=lfs -text
+*.zip filter=lfs diff=lfs merge=lfs -text
diff --git a/.gitignore b/.gitignore
index b63e10b..2f69c07 100644
--- a/.gitignore
+++ b/.gitignore
@@ -287,3 +287,56 @@ poetry.toml
 pyrightconfig.json
 
 # End of https://www.toptal.com/developers/gitignore/api/pycharm,python
+
+# Prerequisites
+*.d
+
+# Object files
+*.o
+*.ko
+*.obj
+*.elf
+
+# Linker output
+*.ilk
+*.map
+*.exp
+
+# Precompiled Headers
+*.gch
+*.pch
+
+# Libraries
+*.lib
+*.a
+*.la
+*.lo
+
+# Shared objects (inc. Windows DLLs)
+*.dll
+*.so
+*.so.*
+*.dylib
+
+# Executables
+*.exe
+*.out
+*.app
+*.i*86
+*.x86_64
+*.hex
+
+# Debug files
+*.dSYM/
+*.su
+*.idb
+*.pdb
+
+# Kernel Module Compile Results
+*.mod*
+*.cmd
+.tmp_versions/
+modules.order
+Module.symvers
+Mkfile.old
+dkms.conf
diff --git a/honeypot/.idea/honeypot.iml b/honeypot/.idea/honeypot.iml
index 74d515a..9a323b0 100644
--- a/honeypot/.idea/honeypot.iml
+++ b/honeypot/.idea/honeypot.iml
@@ -4,7 +4,7 @@
     <content url="file://$MODULE_DIR$">
       <excludeFolder url="file://$MODULE_DIR$/venv" />
     </content>
-    <orderEntry type="inheritedJdk" />
+    <orderEntry type="jdk" jdkName="Python 3.10" jdkType="Python SDK" />
     <orderEntry type="sourceFolder" forTests="false" />
   </component>
 </module>
\ No newline at end of file
diff --git a/honeypot/.idea/misc.xml b/honeypot/.idea/misc.xml
index 46c5297..b7ea289 100644
--- a/honeypot/.idea/misc.xml
+++ b/honeypot/.idea/misc.xml
@@ -1,6 +1,6 @@
 <?xml version="1.0" encoding="UTF-8"?>
 <project version="4">
-  <component name="ProjectRootManager" version="2" project-jdk-name="Python 3.10 (honeypot)" project-jdk-type="Python SDK" />
+  <component name="ProjectRootManager" version="2" project-jdk-name="Python 3.10" project-jdk-type="Python SDK" />
   <component name="PyCharmProfessionalAdvertiser">
     <option name="shown" value="true" />
   </component>
diff --git a/ssh-low-interaction/Dockerfile b/ssh-low-interaction/Dockerfile
index 88864f8..a5d348e 100644
--- a/ssh-low-interaction/Dockerfile
+++ b/ssh-low-interaction/Dockerfile
@@ -1,51 +1,37 @@
-# syntax=docker/dockerfile:1
+# Stage 1: Build dependencies
+FROM python:3.12.0-slim AS builder
 
-# Set the base Python version for the image
-ARG PYTHON_VERSION=3.12.0
-FROM python:${PYTHON_VERSION}-slim as base
+WORKDIR /app
 
-# Prevent Python from writing pyc files.
-ENV PYTHONDONTWRITEBYTECODE=1
+COPY requirements.txt /app/
+RUN pip install --trusted-host pypi.python.org -r requirements.txt
 
-# Keep Python from buffering stdout and stderr.
-ENV PYTHONUNBUFFERED=1
+# Stage 2: Create the final image
+FROM python:3.12.0-slim
 
-# Set the working directory in the container
 WORKDIR /app
 
-# Create a non-privileged user that the app will run under.
-# See https://docs.docker.com/go/dockerfile-user-best-practices/
-ARG UID=10001
-RUN adduser \
-    --disabled-password \
-    --gecos "" \
-    --home "/nonexistent" \
-    --shell "/sbin/nologin" \
-    --no-create-home \
-    --uid "${UID}" \
-    appuser
-
-# Download dependencies as a separate step to take advantage of Docker's caching.
-# Leverage a cache mount to /root/.cache/pip to speed up subsequent builds.
-# Leverage a bind mount to requirements.txt to avoid having to copy them into
-# this layer.
-RUN --mount=type=cache,target=/root/.cache/pip \
-    --mount=type=bind,source=requirements.txt,target=requirements.txt \
-    python -m pip install -r requirements.txt
-
-# Switch to the non-privileged user to run the application.
-USER appuser
-
-# Copy the source code into the container.
-COPY . .
-
-# Expose the port that the application listens on.
-EXPOSE 5000
-
-# Install paramiko for SSH server
-RUN --mount=type=cache,target=/root/.cache/pip \
-    --mount=type=bind,source=requirements.txt,target=requirements.txt \
-    python -m pip install paramiko
-
-# Run the application.
-CMD python -m flask run --host=0.0.0.0
+# Copy only necessary files from the builder stage
+COPY --from=builder /usr/local/lib/python3.12/site-packages /usr/local/lib/python3.12/site-packages
+COPY . /app
+
+# Install additional dependencies, including paramiko
+RUN apt-get update && \
+    apt-get install -y \
+    build-essential \
+    libssl-dev \
+    libffi-dev && \
+    pip install --upgrade pip && \
+    pip install paramiko && \
+    apt-get remove -y build-essential libssl-dev libffi-dev && \
+    apt-get autoremove -y && \
+    rm -rf /var/lib/apt/lists/*
+
+# Make port 2222 available to the world outside this container
+EXPOSE 2222
+
+# Define environment variable
+ENV NAME World
+
+# Run app.py when the container launches
+CMD ["python", "./app.py"]
diff --git a/ssh-low-interaction/app.py b/ssh-low-interaction/app.py
index 8979fb5..621ce68 100644
--- a/ssh-low-interaction/app.py
+++ b/ssh-low-interaction/app.py
@@ -1,8 +1,124 @@
-from flask import Flask
+import paramiko
+import socket
+import threading
+import random
+import string
+import hashlib
 
-app = Flask(__name__)
+def generate_random_string():
+    # Function to generate a random string of 10 characters
+    return ''.join(random.choice(string.ascii_letters) for _ in range(10))
 
 
-@app.route('/')
-def hello_world():
-    return 'Hello, Docker!'
+class SSHServer(paramiko.ServerInterface):
+    def __init__(self):
+        self.event = threading.Event()
+        self.fake_users = self.generate_fake_users(10)
+        print("Fake Users:", self.fake_users)  # Print fake users during initialization
+
+    def generate_fake_users(self, num_users):
+        # Function to generate fake users with predefined strong passwords
+        strong_passwords = [
+            "p@ssw0rd1",
+            "SecureP@ss",
+            "Str0ngPwd!",
+            "An0therPwd!",
+            "Passw0rd123",
+            "SuperSecr3t",
+            "PwdWithSym#",
+            "RandomPwd!",
+            "SecretP@ss",
+            "NewPwd2023",
+            # Add more strong passwords as needed
+        ]
+
+        # List of names for usernames
+        names = ["Alice", "Bob", "Charlie", "David", "Eve", "Frank", "Grace", "Henry", "Ivy", "Jack"]
+
+        fake_users = {}
+        for i in range(num_users):
+            # Use a name as the username
+            username = names[i % len(names)]
+            # Use a password from the predefined list
+            password = strong_passwords[i % len(strong_passwords)]
+            fake_users[username] = password
+
+        return fake_users
+
+    def check_channel_request(self, kind, chanid):
+        if kind == "session":
+            return paramiko.OPEN_SUCCEEDED
+        return paramiko.OPEN_FAILED_ADMINISTRATIVELY_PROHIBITED
+
+    def check_auth_password(self, username, password):
+        if username in self.fake_users:
+            # Hash the provided password and compare it with the stored hashed password
+            hashed_password = hashlib.sha256(password.encode()).hexdigest()
+            if hashed_password == self.fake_users[username]:
+                return paramiko.AUTH_SUCCESSFUL
+        return paramiko.AUTH_FAILED
+
+    def check_auth_publickey(self, username, key):
+        return paramiko.AUTH_SUCCESSFUL
+
+    def get_allowed_auths(self, username):
+        return "password,publickey"
+
+
+def handle_connection(client):
+    # Function to handle each incoming connection
+    transport = paramiko.Transport(client)
+    transport.add_server_key(paramiko.RSAKey(filename="test_rsa.key"))
+    server = SSHServer()
+    transport.start_server(server=server)
+
+    # Accept an incoming channel (SSH session) within 1 second
+    channel = transport.accept(1)
+    if channel is None:
+        print("No channel.")
+        return
+
+    # Wait for the authentication to complete within 10 seconds
+    server.event.wait(10)
+    if not channel.active:
+        print("Client didn't finish auth, disconnecting.")
+        channel.close()
+        return
+
+    # Send random data to the client every 10 seconds while the channel is active
+    while channel.active:
+        data = generate_random_string()
+        try:
+            channel.send(data)
+            print(f"Sent: {data}")
+            server.event.wait(10)
+        except (socket.error, EOFError):
+            break
+
+    transport.close()
+
+def main():
+    # Set up a TCP server socket
+    server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
+    server.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
+    server.bind(("0.0.0.0", 2222))  # Corrected port to 2222
+    server.listen(100)
+
+    # Create an instance of SSHServer to print fake users
+    fake_users_instance = SSHServer()
+
+    # Print the fake users
+    print("Fake Users:")
+    for username, password in fake_users_instance.fake_users.items():
+        print(f"Username: {username}, Password: {password}")
+
+    print("Listening for connections on port 2222...")
+
+    while True:
+        # Accept incoming connections and start a new thread to handle each connection
+        client, addr = server.accept()
+        print(f"Accepted connection from {addr[0]}:{addr[1]}")
+        threading.Thread(target=handle_connection, args=(client,)).start()
+
+if __name__ == "__main__":
+    main()
diff --git a/ssh-low-interaction/compose.yaml b/ssh-low-interaction/compose.yaml
index 94e7d9c..1fe1991 100644
--- a/ssh-low-interaction/compose.yaml
+++ b/ssh-low-interaction/compose.yaml
@@ -14,6 +14,12 @@ services:
     ports:
       - 5000:5000
 
+  ssh-honeypot:
+    build:
+      context: .
+    ports:
+      - 2222:2222
+
 # The commented out section below is an example of how to define a PostgreSQL
 # database that your application can use. `depends_on` tells Docker Compose to
 # start the database before your application. The `db-data` volume persists the
diff --git a/ssh-low-interaction/docker_ssh_server.py b/ssh-low-interaction/docker_ssh_server.py
deleted file mode 100644
index 070b269..0000000
--- a/ssh-low-interaction/docker_ssh_server.py
+++ /dev/null
@@ -1,111 +0,0 @@
-import paramiko
-import socket
-import threading
-import random
-import string
-
-def generate_random_string():
-    # Function to generate a random string of 10 characters
-    return ''.join(random.choice(string.ascii_letters) for _ in range(10))
-
-
-class SSHServer(paramiko.ServerInterface):
-    def __init__(self):
-        self.event = threading.Event()
-        self.fake_users = self.generate_fake_users(10)
-        print("Fake Users:", self.fake_users)  # Print fake users during initialization
-
-    def generate_fake_users(self, num_users):
-        # Function to generate fake users with passwords
-        fake_users = {}
-        for i in range(1, num_users + 1):
-            username = f"fakeuser{i}"
-            # Generate a random password of length 8
-            password = self.generate_random_password(8)
-            fake_users[username] = password
-        return fake_users
-
-    def generate_random_password(self, length):
-        # Function to generate a random password
-        characters = string.ascii_letters + string.digits + string.punctuation
-        return ''.join(random.choice(characters) for _ in range(length))
-
-    def check_channel_request(self, kind, chanid):
-        if kind == "session":
-            return paramiko.OPEN_SUCCEEDED
-        return paramiko.OPEN_FAILED_ADMINISTRATIVELY_PROHIBITED
-
-    def check_auth_password(self, username, password):
-        if username in self.fake_users and password == self.fake_users[username]:
-            return paramiko.AUTH_SUCCESSFUL
-        return paramiko.AUTH_FAILED
-
-    def check_auth_publickey(self, username, key):
-        return paramiko.AUTH_SUCCESSFUL
-
-    def get_allowed_auths(self, username):
-        return "password,publickey"
-
-
-def handle_connection(client):
-    # Function to handle each incoming connection
-    transport = paramiko.Transport(client)
-    transport.add_server_key(paramiko.RSAKey(filename="test_rsa.key"))
-    server = SSHServer()
-    transport.start_server(server=server)
-
-    # Accept an incoming channel (SSH session) within 1 second
-    channel = transport.accept(1)
-    if channel is None:
-        print("No channel.")
-        return
-
-    # Wait for the authentication to complete within 10 seconds
-    server.event.wait(10)
-    if not channel.active:
-        print("Client didn't finish auth, disconnecting.")
-        channel.close()
-        return
-
-    # Send random data to the client every 10 seconds while the channel is active
-    while channel.active:
-        data = generate_random_string()
-        try:
-            channel.send(data)
-            print(f"Sent: {data}")
-            server.event.wait(10)
-        except (socket.error, EOFError):
-            break
-
-    transport.close()
-
-def main():
-    # Set up a TCP server socket
-    server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
-    server.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
-    server.bind(("0.0.0.0", 2222))  # Corrected port to 2222
-    server.listen(100)
-
-    # Create an instance of SSHServer to print fake users
-    fake_users_instance = SSHServer()
-
-    # Print the fake users
-    print("Fake Users:")
-    for username, password in fake_users_instance.fake_users.items():
-        print(f"Username: {username}, Password: {password}")
-
-    print("Listening for connections on port 2222...")
-
-    while True:
-        # Accept incoming connections and start a new thread to handle each connection
-        client, addr = server.accept()
-        print(f"Accepted connection from {addr[0]}:{addr[1]}")
-        threading.Thread(target=handle_connection, args=(client,)).start()
-
-if __name__ == "__main__":
-    main()
-
-
-if __name__ == "__main__":
-    main()
-
